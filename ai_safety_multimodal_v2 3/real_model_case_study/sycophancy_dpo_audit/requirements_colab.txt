# For running pipeline.py in Colab / a GPU machine (the actual DPO+SAE training).
# Do NOT install this on Streamlit Community Cloud -- it has no GPU and will
# fail on bitsandbytes/flash-attn-style builds. Use requirements_streamlit.txt there.
transformers>=4.44
accelerate>=0.33
peft>=0.12
trl>=0.9
sae-lens>=4.0
bitsandbytes>=0.43
huggingface_hub>=0.24
datasets>=2.20
torch>=2.2
